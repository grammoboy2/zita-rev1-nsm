diff a/source/rotary.cc b/source/rotary.cc	(rejected hunks)
@@ -40,7 +40,7 @@ int RotaryCtl::_ry = 0;
 RotaryCtl::RotaryCtl (X_window     *parent,
                       X_callback   *cbobj,
                       RotaryImg    *image,
-		      int  xp,
+                      int  xp,
                       int  yp,
                       int  cbind) :
 
@@ -159,18 +159,18 @@ void RotaryCtl::motion (XMotionEvent *E)
         dx = E->x - _rx;
         dy = E->y - _ry;
         r = handle_motion (dx, dy);
-	if (r)
-	{
+        if (r)
+        {
             callback (r);
-	    render ();
-	}
+            render ();
+        }
     }
 }
 
 
 void RotaryCtl::set_state (int s)
 {
-    _state = s;	
+    _state = s;
     render ();
 }
 
